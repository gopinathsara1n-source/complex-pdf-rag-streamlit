# Example PDF

Place these preprocessed artifacts here:

- `manifest.json`
- `chunks.json`
- `index.faiss`

The manifest must contain a `document_id` matching the directory/artifacts
used by the app, or the example section can be adapted to point to a chosen
storage directory.
